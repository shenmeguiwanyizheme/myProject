<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mapper.user.UserMapper">
    <update id="update" parameterType="com.example.pojo.user.User" statementType="PREPARED">
        update user
        <trim prefix="set" prefixOverrides=",">
            <if test="name!=null">
                name=#{name}
            </if>
            <if test="age!=null">
                , age=#{age}
            </if>
        </trim>
        where id=#{id}
    </update>
    <update id="delete" parameterType="com.example.pojo.user.User" statementType="PREPARED">
        update user set isdeleted=1 where id=#{id}
        <!--        <where>-->
        <!--            <if test="name!=null">-->
        <!--                and name=#{name}-->
        <!--            </if>-->
        <!--            <if test="age!=null">-->
        <!--                and age=#{age}-->
        <!--            </if>-->
        <!--        </where>-->
    </update>
    <select id="queryAll" resultType="com.example.pojo.user.User" statementType="PREPARED">
        select *
        from user
        where isdeleted = 0
    </select>
    <select id="queryByName" resultType="com.example.pojo.user.User" statementType="PREPARED">
        select *
        from user
        where name = #{name}
    </select>
    <insert id="add" parameterType="com.example.pojo.user.User" statementType="PREPARED">
        insert into user
        values (default, #{age}, #{name}, 0)
    </insert>
</mapper>